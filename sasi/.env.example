# SASI — Comando UTI Alpha
# Copie este arquivo pra .env e nunca commite o .env real

VITE_SUPABASE_URL=https://idswehsvvqczzkiatuzu.supabase.co

# ⚠️ IMPORTANTE: use a anon key JWT LEGACY (header começa com "eyJhbGc..."),
# NÃO a publishable key (sb_publishable_*).
#
# Motivo: @supabase/supabase-js@2.45 ainda não reconhece publishable keys.
# Pegue em: Supabase Dashboard → Settings → API → JWT (legacy keys) → "anon public"
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.<...placeholder — substitua...>
